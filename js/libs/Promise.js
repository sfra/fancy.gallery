define([], function () {

    define([], function () {});

    var _Promise;
    if (typeof Promise === "undefined" || Promise.toString().indexOf("[native code]") === -1) {

    } else {
        
        _Promise = Promise;
    }


    return _Promise;
});